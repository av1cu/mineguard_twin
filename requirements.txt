fastapi>=0.100.0
uvicorn>=0.22.0
openmines>=0.1.0
opencv-python-headless>=4.8.0
mediapipe>=0.10.0
pydantic>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0
requests>=2.31.0
pillow>=10.0.0
pytest>=7.4.0

# AI Agent layer (Decision Support System, on top of existing architecture)
# NOTE: intentionally NOT using the `openai` package here — openmines pins
# openai==0.27.8 (pre-v1 SDK), which conflicts with the v1 client API our
# agent needs. agent/llm_client.py talks to the OpenAI-compatible endpoint
# directly via httpx instead (see comment in that file).
httpx>=0.27.0
pydantic-settings>=2.5.2
python-dotenv>=1.0.0
